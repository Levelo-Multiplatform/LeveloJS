import type { NativeRendererBridge } from "./NativeRendererBridge.js";

/**
 * Creates a native renderer bridge.
 */
export function createNativeRenderer(
  bridge: NativeRendererBridge,
): NativeRendererBridge {
  return bridge;
}
